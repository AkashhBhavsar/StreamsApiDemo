package test;

import java.util.ArrayList;
import java.util.List;
import java.util.Map;
import java.util.stream.Collectors;

public class MainClass {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		List<Employee> empList = new ArrayList<>();
		
		/*
		 * empList.add(new Employee(1, "Akash", 8)); empList.add(new Employee(2,
		 * "Akash", 9)); empList.add(new Employee(3, "XYZ", 5)); empList.add(new
		 * Employee(4, "ABCD", 7));
		 * 
		 * empList.stream().sorted().collect(Collectors.toList());
		 * 
		 * Map<String, Long> result = empList.stream().collect(null)
		 */
		
	}

}
